import React from 'react';

const LoginHeader: React.FC = () => {
    return (
        <header className="login-header">
            <h1><span className="icon"></span>Studiverse</h1>
        </header>
    );
};

export default LoginHeader;